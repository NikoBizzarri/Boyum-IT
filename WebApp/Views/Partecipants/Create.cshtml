@model WebApp.Models.PartecipantViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Partecipant</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Partecipant.Fullname" class="control-label"></label>
                <input asp-for="Partecipant.Fullname" class="form-control" />
                <span asp-validation-for="Partecipant.Fullname" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Partecipant.Email" class="control-label"></label>
                <input asp-for="Partecipant.Email" class="form-control" />
                <span asp-validation-for="Partecipant.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Partecipant.Phone" class="control-label"></label>
                <input asp-for="Partecipant.Phone" class="form-control" />
                <span asp-validation-for="Partecipant.Phone" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Partecipant.EventId" class="control-label"></label>
                <select id="drpEmpList" class="form-control" asp-for="Partecipant.EventId">
                    <option value=@(Model.Events[0].Name)>@(Model.Events[0].Name)</option>
                    <option value=@(Model.Events[1].Name)>@(Model.Events[1].Name)</option>
                    <option value=@(Model.Events[2].Name)>@(Model.Events[2].Name)</option>
                </select>
                <input type="hidden" asp-for="Partecipant.EventId" />

            </div>
            <div class="form-group">
                <input type="submit" value="Register" class="btn btn-primary" />
            </div>
        </form>
        <form asp-action="List">

        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

